#!/bin/sh

if [ -z "$FACTORIO" ]; then
    echo "FACTORIO environment variable is not defined"
    exit 1
fi

configpath=$(dirname $FACTORIO)/../../config/config.ini

sed -i "s/locale=.*/locale=ja/" $configpath
$FACTORIO "$@"
sed -i "s/locale=ja/locale=/" $configpath
