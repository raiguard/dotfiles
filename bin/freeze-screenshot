#!/bin/sh

grim -c -o $(swaymsg -t get_outputs | jq -r '.[] | select(.focused) | .name') /tmp/freeze-screenshot
swayimg -f /tmp/freeze-screenshot &
grimshot --notify "$1" area
killall swayimg # We are only using swayimg for this purpose
