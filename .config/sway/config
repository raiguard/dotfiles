# Read `man 5 sway` for a complete reference.

### Variables

set $alt Mod1
set $mod Mod4

set $left h
set $down j
set $up k
set $right l

### Autostart

exec autotiling
exec blueman-applet
exec dunst
exec nm-applet --indicator
exec pasystray
exec udiskie --tray
exec /usr/bin/polkit-dumb-agent
exec wlsunset -l 40.3 -L -111.8 -t 2500

### GTK3 workaround

exec_always import-gsettings

### Output configuration

# Screens
output DP-1 position 2560,0 bg ~/pictures/wallpapers/1004356.png fill
output DP-2 position 0,0 bg ~/pictures/wallpapers/1141314.png fill
output HDMI-A-1 disable scale 2 bg ~/pictures/wallpapers/1225904.jpg fill

### Idle configuration

exec swayidle -w \
         timeout 300 'swaylock -f' \
         timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
         before-sleep 'swaylock -f'

### Key bindings

# Start a terminal
bindsym $mod+Return exec kitty

# Kill focused window
bindsym $mod+backspace kill

# Drag floating windows by holding down $mod and left mouse button.
# Resize them with right mouse button + $mod.
# Despite the name, also works for non-floating windows.
# Change normal to inverse to use left mouse button for resizing and right
# mouse button for dragging.
floating_modifier $mod normal

# Reload the configuration file
bindsym $mod+Shift+c reload

# Exit sway (logs you out of your Wayland session)
bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Rofi
bindsym $mod+r exec rofi -show drun
bindsym $mod+w exec rofi -show window
bindsym $mod+q exec rofi -show power-menu -modi 'power-menu:~/.local/bin/rofi-power-menu --choices=logout/reboot/shutdown'

# Moving around:

# Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# Move the focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# Resize the focused window
bindsym $mod+Control+$left resize shrink width 30px
bindsym $mod+Control+$down resize grow height 30px
bindsym $mod+Control+$up resize shrink height 30px
bindsym $mod+Control+$right resize grow width 30px

# Workspaces:

# Switch to workspace
bindsym $mod+u workspace number 1
bindsym $mod+i workspace number 2
bindsym $mod+o workspace number 3
bindsym $mod+p workspace number 4
bindsym $mod+7 workspace number 5
bindsym $mod+8 workspace number 6
bindsym $mod+9 workspace number 7
bindsym $mod+0 workspace number 8
# Move focused container to workspace
bindsym $mod+Shift+u move container to workspace number 1
bindsym $mod+Shift+i move container to workspace number 2
bindsym $mod+Shift+o move container to workspace number 3
bindsym $mod+Shift+p move container to workspace number 4
bindsym $mod+Shift+7 move container to workspace number 5
bindsym $mod+Shift+8 move container to workspace number 6
bindsym $mod+Shift+9 move container to workspace number 7
bindsym $mod+Shift+0 move container to workspace number 8
# Move windows between monitors
bindsym $mod+Shift+period move window to output DP-1
bindsym $mod+Shift+comma move window to output DP-2
bindsym $mod+Shift+slash move window to output HDMI-A-1
# Move workspaces between monitors
bindsym $mod+Control+period move workspace to output DP-1
bindsym $mod+Control+comma move workspace to output DP-2
bindsym $mod+Control+slash move workspace to output HDMI-A-1

# Layout stuff:

# You can "split" the current object of your focus with
# $mod+b or $mod+v, for horizontal and vertical splits
# respectively.
bindsym $mod+b splith
bindsym $mod+v splitv

# Make the current focus fullscreen
bindsym $mod+f fullscreen

# Toggle the current focus between tiling and floating mode
bindsym $mod+Shift+space floating toggle

# Swap focus between the tiling area and the floating area
bindsym $mod+space focus mode_toggle

# Move focus to the parent container
bindsym $mod+a focus parent

# Scratchpad:

# Sway has a "scratchpad", which is a bag of holding for windows.
# You can send windows there and get them back later.

# Move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+minus scratchpad show

# Toggle autotiling
bindsym $mod+t exec ~/.config/sway/toggle-autotiling

# Lock

bindsym $mod+Alt+l exec swaylock

# Media

bindsym --locked XF86AudioRaiseVolume exec sh /home/rai/.config/sway/volume i
bindsym --locked XF86AudioLowerVolume exec sh /home/rai/.config/sway/volume d
bindsym --locked XF86AudioPlay exec sh /home/rai/.config/sway/media play-pause
bindsym --locked XF86AudioNext exec sh /home/rai/.config/sway/media next
bindsym --locked XF86AudioPrev exec sh /home/rai/.config/sway/media previous

# Screenshots

# Select region
bindsym print exec grim -g "$(slurp -b 00000066 -c e5c07b)" - | wl-copy
# Focused  monitor
bindsym Shift+print exec grim -o $(swaymsg -t get_outputs | jq -r '.[] | select(.focused) | .name') - | wl-copy; notify-send "Screen captured"
# Focused  window
bindsym Control+print exec grim -g "$(swaymsg -t get_tree | jq -j '.. | select(.type?) | select(.focused).rect | "\(.x),\(.y) \(.width)x\(.height)"')" - | wl-copy; notify-send "Window captured"

### Looks

default_border pixel 2
default_floating_border pixel 2

gaps inner 6

client.focused #56b6c2 #56b6c2 #abb2bf #e5c07b
client.unfocused #70798a #70798a #abb2bf #e5c07b
client.urgent #e06c75 #70798a #abb2bf #e5c07b

### Rules

for_window [class="factorio"] move window to workspace number 3
for_window [class="discord"] move window to workspace number 4
for_window [app_id="obs"] move window to workspace number 4

### Other

bar swaybar_command waybar

include /etc/sway/config.d/*
